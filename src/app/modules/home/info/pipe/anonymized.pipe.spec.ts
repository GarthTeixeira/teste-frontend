import { AnonymizedPipe } from './anonymized.pipe';

describe('AnonymizedPipe', () => {
  it('create an instance', () => {
    const pipe = new AnonymizedPipe();
    expect(pipe).toBeTruthy();
  });
});
